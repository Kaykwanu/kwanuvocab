# Kwanu GRE Vocab

An interactive GRE Vocabulary builder and quiz app featuring root breakdown analytics and study modes.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
